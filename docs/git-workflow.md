# Git workflow

`main` represents production and `develop` is the integration/staging branch. Use short-lived branches prefixed with `feat/`, `fix/`, `docs/`, `chore/`, `refactor/`, `release/` or `hotfix/`.

Use Conventional Commits in English, such as `feat(connectors): add OpenAI usage synchronization`. Do not create a permanent `staging` branch.
