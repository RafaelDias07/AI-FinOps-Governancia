{
  "name": "ai-finops-governance",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"npm run dev -w @finops/web\" \"npm run start:dev -w @finops/api\"",
    "build": "npm run build -w @finops/shared && npm run build -w @finops/database && npm run build -w @finops/connectors && npm run build -w @finops/api && npm run build -w @finops/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run build -w @finops/shared && npm run build -w @finops/database && npm run build -w @finops/connectors && npm run typecheck -w @finops/api && npm run typecheck -w @finops/web",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:generate": "npm run prisma:generate -w @finops/database"
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.0",
    "eslint": "^9.16.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  }
}
